version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    environment:
      - APP_ENV=development
  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: candyland
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
